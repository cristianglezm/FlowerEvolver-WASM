#ifndef FLOWER_EVOLVER_CONFIG_HPP
#define FLOWER_EVOLVER_CONFIG_HPP

#include <string>

// Generated by CMake (configure_file()) from config.hpp.template -- do not
// edit the generated config.hpp directly, edit this template instead.
// Source of truth for the version is package.json; see CMakeLists.txt.

#if defined(_WIN32) && !defined(FE_STATIC)
    #ifdef FE_EXPORTS
        #define FE_API __declspec(dllexport)
    #else
        #define FE_API __declspec(dllimport)
    #endif
#elif defined(__GNUC__) && !defined(FE_STATIC)
    #define FE_API __attribute__((visibility("default")))
#else
    #define FE_API
#endif

namespace fe{
    /**
     * @brief library version, mirrors package.json's "version" field exactly.
     * @return e.g. "3.0.0"
     */
    inline std::string version(){
        return "@FE_VERSION@";
    }

    /**
     * @brief short git commit hash this build was made from.
     * @return e.g. "a1b2c3d", or "unknown" when not built from a git checkout
     * (for example, a downloaded source archive rather than a git clone).
     */
    inline std::string commitHash(){
        return "@FE_COMMIT_HASH@";
    }

    /**
     * @brief version() and commitHash() combined using SemVer build-metadata
     * syntax ('+', not '-', which SemVer reserves for pre-release tags).
     * @return e.g. "3.0.0+a1b2c3d"
     */
    inline std::string versionString(){
        return version() + "+" + commitHash();
    }
}

#endif // FLOWER_EVOLVER_CONFIG_HPP
